# Changelog

所有重要的變更都會記錄在此檔案中。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號遵循 `{PZ版本}-{主版本}.{次版本}.{修訂}` 格式。

## [42.20.4-0.4.0] - 2026-09-13

### 新增

- 公告面板新增「語音」按鈕與語系圖示，可選中文、英文、日文、自動或原提示音；選擇會記憶，且不影響公告文字語系。預設仍使用原提示音，更新後請重啟客戶端。
- 公告面板新增音量滑桿與百分比，和遊戲選項共用同一份音量設定；放開滑桿或選擇語音時立即試聽，切換時停止上一段，避免聲音重疊。既有靜音開關同樣適用。

## [42.20.4-0.3.1] - 2026-09-13

### 修正

- 修正死亡後直接以新角色繼續遊戲時，公告浮動圖標消失；重生後恢復原圖標與拖曳位置，更新後請重啟客戶端。
- 修正公告浮動圖標重新進入遊戲後忘記位置；現在拖曳放開便立即保存，切換解析度也會還原各自的位置，不必等離開遊戲。

## [42.20.4-0.3.0] - 2026-08-30

### 新增

- **公告可以分分類了**：服主可在 `NoticeBoard/categories.txt` 宣告分類，再把公告放進各語系的分類目錄；分類名稱可依公告語系顯示不同文字
- **公告改用文件樹側欄**：分類與公告改為左側可展開清單，公告再多也不必猜還有沒有藏在畫面外的頁籤；分類與單則公告都會顯示未讀紅點
- **目錄與工具列有圖示了**：分類、公告與工具列按鈕都有一致的小圖示；圖示缺失或前置版本較舊時自動退回文字，操作不受影響
- **一鍵重建範例公告（管理員限定）**：從面板選繁體中文或 English 後，直接在正式公告目錄重建完整語法示範、歡迎公告、`.only` 規則範本與根層說明。每次只覆寫固定 5 個路徑，選單會先警告 `categories.txt` 將被範例分類取代
- **文件樹批次展開／收合**：左側工具列新增「全部展開」與「全部收合」，公告再多也不用逐個點擊分類

### 變更

- **移動分類不再驚動全服**：公告的穩定 ID 仍是裸檔名，只搬資料夾、不改檔名或內容時，玩家的已讀狀態與本機內容快取都會保留
- **面板一開就更大、目錄預設打開**：預設尺寸提高為螢幕 72%×88%（1080p 約 1382×950，上限 1440×1080）；已記住舊尺寸的玩家可按「重設大小」套用
- **窄視窗自動保住內文**：面板寬度低於 640px 時側欄自動收合；玩家手動選擇會記在 `settings.ini`，拉寬後恢復
- **封面換新**：Workshop 與遊戲內封面改用新公告板＋粉色貓娘設計；因 PZ 封面必須是正方形，完整原圖等比縮放後只補上下純黑邊，不裁切、不拉伸

### 修正

- **目錄捲軸出現時未讀紅點不再被切掉**：公告多到目錄要捲動時，右側紅點原本會被捲軸的裁切邊界吃掉，只剩一條細線

> 技術要點：PZ 的 `listFilesInZomboidLuaDirectory` 排除目錄且不遞迴，故分類必須先由根層索引宣告；manifest 以加法欄位 `cats`／`c` 傳送分類，舊 client/server 缺欄位時一律退回未分類。DefaultLanguage 仍是 fallback 與分類位置的權威，玩家語系只覆蓋同 ID 的內容。圖示走前置 MOD 的 `MinidoracatUI.v1.Icons`（API v1 rev>=2，`get`/`draw` 皆 fail-soft 回 nil／false），資產是純白 PNG、運行時染成該列文字色；工具列用原生 `ISButton.iconTexture` ＋ `joypadTextureWH=14`，有圖示才把按鈕寬加上 `14 + 原生固定間距 5`。紅點被裁掉的根因是 `ISScrollingListBox:prerender` 在捲軸可見時把 stencil 右緣收到 `vscroll.x + 3`（= 寬-13），而紅點畫在 `寬-14`；修正是捲軸可見時文字留白與紅點同時左移 17px（vscroll 實際寬度）。範例內容全部放在 MOD 資源檔 `42/media/NoticeBoardExamplePack/`（共用 2 檔＋每語系 3 檔；Kahlua 會把 `.lua` 內的非 ASCII 字面值截成單一位元組，中文範例只能走 `getModFileReader` 的 UTF-8 路徑），單次請求只讀寫其中 5 個。落地是「先把該次的 5 份資源全部讀進記憶體、全到齊才動磁碟」的兩階段，逐檔沿用既有 `writeTextFile` 的寫後讀回驗證（`PrintWriter` 從不外拋 IOException，讀回比對是分辨真落地與假成功的唯一方法）；寫入順序固定為「選定語系三檔 → `categories.txt` → `README.txt`」，讓最可能的部分寫入狀態落在「新公告＋舊分類」而不是「分類設定先被清掉」。全部寫成功後才 `refreshSnapshot`，refresh 回 false 一律記 log 並回 `failed`，不宣稱成功。受管清單是 `.lua` 內的固定相對路徑，不列舉磁碟、也沒有任何刪除路徑。協定是 client `examples`（帶 `lang`）→ server `examplesResult`（payload 只有 enum `kind` 與成功時固定為 5 的整數 `count`；`lang` 兩端都只接受精確的 `CH`／`EN`，其他值零寫入、零冷卻消耗並節流記 log，伺服器絕對路徑與原始 IO 錯誤只留在 server log）。

## [42.20.2-0.2.0] - 2026-08-25

### 新增

- **連結先看再點**：滑鼠移到連結上會浮出完整網址，點下去之前就知道要複製的是什麼——公告裡的顯示文字與實際網址不一致時一眼識破
- **連結會亮起來**：滑鼠壓在連結上時顏色明顯變亮，看得出這是可以點的

### 變更

- **公告欄按鈕換新圖標**：畫面上的浮動按鈕從驚嘆號改成喇叭圖——公告的通用符號，一眼看懂
- **行內程式碼與中文之間有間距了**：`檔名.png` 這類標記不再與前後的中文黏成一團，顯示時也保留反引號當邊界，一眼看出哪段是要照打的內容
- **首次啟動的範例公告改版**：示範圖改用 MOD 自帶的吉祥物圖（不再借用遊戲內建貼圖）；「放自己的圖」的說明改為主推 `NoticeBoard/images/` 自動同步（舊文案還在教服主做貼圖包 MOD，那已經不需要了）；連結範例改用 Discord 邀請連結，順帶示範點擊複製的行為

### 修正

- **捲動後連結點不準**：公告捲動過之後，滑鼠要移到連結上方一段距離才會觸發——現在 hover 與點擊都精準落在文字上

> 技術要點：行內程式碼的間距是在上色區內側墊 U+00A0——Kahlua 的 `string.char` 參數是 UTF-16 code unit，必須寫 `string.char(0xA0)` 而非 UTF-8 位元組拆分（詳見 AGENTS.md Kahlua 段新增的三條編碼坑）。連結判定偏移的根因是捲動補償重複：`getMouseX/Y` 與 `onMouseUp` 參數都已是內容座標（`ISUIElement.lua:339-350`、`UIElement.java:1311-1321`），`findLinkAt` 不得再加 `getYScroll()`。浮鈕圖標 `nb_megaphone.png` 存 48px 顯 24px（2:1 縮放在 GL_LINEAR 下最銳利）；彩圖經 `drawTextureScaled` 需 r/g/b 全傳 1 保留原色。

## [42.20.2-0.1.0] - 2026-08-25

### 新增

- **初始版本**：遊戲內公告欄，直接編輯 Markdown 檔案即可更新（依伺服器輪詢間隔生效，預設 60 秒），多份檔案自動分頁
- **公告面板**：可拖曳的浮窗按鈕開關面板；視窗大小與位置自動記憶，另有「重設大小」按鈕還原成畫面比例的預設尺寸
- **Markdown 渲染**：標題、粗體、斜體、行內程式碼、清單、分隔線、連結與圖片（支援的語法與已知差異列在 `docs/ADMIN_GUIDE.md`）
- **圖片**：伺服器把 `NoticeBoard/images/` 的 PNG 分批同步到玩家本機快取，亦可改用圖包 MOD 的貼圖路徑；支援 `![alt](path =600x200)` 尺寸語法（`=600x`／`=x200` 依原圖比例補另一邊），指定值會被夾在面板可用寬度內
- **換圖自動生效**：放進新檔名的圖下一輪輪詢就生效；**原地覆蓋同名檔案**也會自動偵測（比對檔案大小），不必再按「重新載入」。新舊檔位元組數剛好相同時仍需手動重新載入
- **images 資料夾說明檔**：伺服器啟動時在 `NoticeBoard/images/` 放一份四語（EN／繁中／简中／日本語）`00_README.txt`，寫明檔名規則、容量上限、公告內引用寫法與更新機制；已存在則絕不覆蓋
- **圖片上限可由服主調整**：張數與總量從寫死改為 Sandbox 選項 `MaxImageCount`（1–200，預設 20）與 `MaxImageTotalKB`（4096–16384 KB，預設 4096），連同既有的 `MaxImageKB`（64–4096 KB，預設 512）共三項。**預設值即舊行為，什麼都不設完全不變**；超限的 log 會一併寫出當下生效的上限。客戶端保有不受伺服器設定影響的保護上限（接收端張數／單張大小／總量天花板、分塊數必須與宣告位元組數相符，以及快取總量與寫入速率上限）
- **圖片快取有界且會自動淘汰**：快取以連線的伺服器位址分開命名空間（不同伺服器不共用檔案，避免內容雜湊碰撞被拿來換掉別台伺服器的圖），總量上限 128 MB，超過時以最久沒用到的優先、在背景分批淘汰，目前公告用到的圖不會被動到。另有每 30 分鐘 64 MB 的寫入速率上限，撞到只影響當下的時間窗、下一個窗自動恢復
- **圖片同步中斷會續傳**：傳輸卡住時保留已收到的分塊，重試只向伺服器要「第一個缺的分塊」之後的部分，不再整張從頭重來（同一次遊戲程序內有效；離開遊戲後沒下載完的圖仍會重新下載，引擎的檔案輸出沒有 append 能力）。拼回來的內容照樣要通過完整的內容雜湊驗證才會採用。撞到寫入速率上限時，玩家會看到一則「同步已暫停、約 30 分鐘後恢復」的遊戲內提示，一個時間窗最多一則
- **連結**：點擊複製網址到剪貼簿，無效 URL 一律擋下
- **多語系**：伺服器依 `NoticeBoard/<LANG>/` 分語系目錄供稿，找不到時回退到預設語系；玩家可在面板自行選擇公告語系（含「自動（跟隨遊戲語系）」），選擇記在本機 `NoticeBoard/settings.ini`，下次進場沿用。伺服器掃到的語系目錄清單有變化時會寫一行 log，服主據此就知道該把預設語系設成什麼
- **語系限定公告**：檔名加上 `.only` 標記（例如 `EN/20_en_notice.only.txt`）即代表「只給這個語系目錄的玩家看」，不會被鋪成其他語系的底稿
- **更新通知**：以內容 hash 判定未讀，公告改動時跳 Toast、浮窗顯示紅點、對應頁籤標記未讀
- **新公告提示音**：有新公告或公告更新時，除了畫面上的提示訊息再播一聲 MOD 自帶的提示音（音檔可由服主自行替換，見 `docs/ADMIN_GUIDE.md`「換掉提示音」）。**一批只響一次**，不會每則公告都響；換公告語系不響（那不是新內容）
- **玩家端音效設定**：遊戲的「選項 → MODS」分頁有本 MOD 的提示音開關與音量滑桿（0-100，預設 60）。MOD 自帶音效不受遊戲內建的音效音量影響，這是唯一調得動它的地方；沙盒選項「新公告播放提示音」是服主端總開關，兩者是 AND 關係
- **沙盒選項**：自動彈出模式（一律／僅未讀／不彈出）、輪詢間隔、預設語系（29 個 PZ 語系代碼下拉）、新公告播放提示音、單張圖片大小上限、圖片張數上限、圖片總量上限
- 多人與單機皆支援

> 技術要點：提示音走 `GameSounds` 的 non-bank fallback（`media/sound/` 正下方、檔名即音效名），不需要 FMOD bank；玩家端音量走 PZ 原生 `PZAPI.ModOptions`（值存 `Zomboid/Lua/ModOptions.ini`），因為非 bank 音效不經 `vca:/Settings_Sfx`、遊戲內建音效音量對它無效。`.only` 標記的語意獨立於 `DefaultLanguage`，預設語系目錄內也能表達限定。
