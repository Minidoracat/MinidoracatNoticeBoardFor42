# Changelog

所有重要的變更都會記錄在此檔案中。

格式基於 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號遵循 `{PZ版本}-{主版本}.{次版本}.{修訂}` 格式。

## [未發布]

### 新增

- **新公告提示音**：有新公告或公告更新時，除了畫面上的提示訊息再播一聲 MOD 自帶的提示音（走 `GameSounds` 的 non-bank fallback，不需要 FMOD bank；音檔可由服主自行替換，見 `docs/ADMIN_GUIDE.md`「換掉提示音」）。**一批只響一次**，不會每則公告都響；換公告語系不響（那不是新內容）
- **玩家端音效設定**：遊戲的「選項 → MODS」分頁新增本 MOD 的開關與音量滑桿（0-100，預設 60），走 PZ 原生的 `PZAPI.ModOptions`、值存在 `Zomboid/Lua/ModOptions.ini`。MOD 自帶音效不受遊戲內建的音效音量影響，這是唯一調得動它的地方；沙盒選項「新公告播放提示音」是服主端總開關，兩者是 AND 關係

### 變更

- **預設公告語言改成下拉選單**：沙盒選項 `DefaultLanguage` 從自由輸入的字串改成 29 個 PZ 語系代碼的下拉（`enum`），不必再記大小寫、也不會打錯字後靜默退回 `EN`。清單是「MOD 接受的代碼」而非「伺服器實際有的目錄」——沙盒選項在載入期就解析完，沒有執行期偵測的機制。舊的字串 ini 值仍然相容
- **新增偵測結果的 log**：伺服器掃到的語系目錄清單變化時寫一行 `notice languages=<清單> default=<語系>`，服主據此就知道該把 `DefaultLanguage` 設成什麼（玩家端則是面板的「語言」選單，那份一直都是偵測式的）

- **公告面板預設更大**：預設尺寸從螢幕 52% × 68% 提高到 62% × 80%，上限同步從 1100 × 820 提到 1280 × 960（1920×1080 下 998 × 734 → 1190 × 864）。**已經開過面板的玩家要按「重設大小」才會套用**——視窗尺寸一旦調整過就記在本機 layout.ini，不會被覆寫
- **切換公告語系更快**：語系切換不再與進場註冊、同步逾時補救共用同一個 10 秒冷卻，改用獨立的 3 秒冷卻。進場後馬上換語系、或連續換兩次語系都不必再等 10 秒

### 修正

- **公告大標題與視窗標題沒對齊**：內容區的置中是算在「扣掉右側捲軸空間」的區間內，而右邊預留 13px 捲軸、左邊沒有，於是 `#` 標題（以及所有置中的行）恆偏左 6.5px，與視窗標題列的置中文字對不齊。改成左右邊界對稱，置中就落在視窗正中；代價是內容左側留白從 10px 變 23px

- **切換語系可能停在舊語系**：切換完成的判定原本只比對請求序號，於是「帶著這一次的序號、內容卻還是舊語系」的快照會被當成切換完成，之後不再重送，玩家就永久停在舊語系（再選同一個語系也會被當成沒變更而吞掉）。改成序號與語系都必須相符
- **切換語系的重送可能讓同步從頭重來**：伺服器對同一位玩家重新排推送任務時會從第一份公告重頭送，內容較多又碰上多人同時同步時可能一直推不完。同一份切換請求已在推送中時不再重排；玩家端也在伺服器已受理後停止重送（分塊沒收齊仍由既有的同步補救機制處理）

## [42.20.2-0.1.0] - 2026-08-09

### 新增

- **初始版本**：遊戲內公告欄，直接編輯 Markdown 檔案即可更新（依伺服器輪詢間隔生效，預設 60 秒），多份檔案自動分頁
- **公告面板**：可拖曳的浮窗按鈕開關面板；視窗大小與位置自動記憶，另有「重設大小」按鈕還原成畫面比例的預設尺寸
- **Markdown 渲染**：標題、粗體、斜體、行內程式碼、清單、分隔線、連結與圖片（支援的語法與已知差異列在 `docs/ADMIN_GUIDE.md`）
- **圖片**：伺服器把 `NoticeBoard/images/` 的 PNG 分批同步到玩家本機快取，亦可改用圖包 MOD 的貼圖路徑；支援 `![alt](path =600x200)` 尺寸語法（`=600x`／`=x200` 依原圖比例補另一邊），指定值會被夾在面板可用寬度內
- **換圖自動生效**：放進新檔名的圖下一輪輪詢就生效；**原地覆蓋同名檔案**也會自動偵測（比對檔案大小），不必再按「重新載入」。新舊檔位元組數剛好相同時仍需手動重新載入
- **images 資料夾說明檔**：伺服器啟動時在 `NoticeBoard/images/` 放一份四語（EN／繁中／简中／日本語）`00_README.txt`，寫明檔名規則、容量上限、公告內引用寫法與更新機制；已存在則絕不覆蓋
- **圖片上限可由服主調整**：張數與總量從寫死改為 Sandbox 選項 `MaxImageCount`（1–200，預設 20）與 `MaxImageTotalKB`（4096–16384 KB，預設 4096），連同既有的 `MaxImageKB`（64–4096 KB，預設 512）共三項。**預設值即舊行為，什麼都不設完全不變**；超限的 log 會一併寫出當下生效的上限。客戶端保有不受伺服器設定影響的保護上限（接收端張數／單張大小／總量天花板、分塊數必須與宣告位元組數相符，以及快取總量與寫入速率上限）
- **圖片快取有界且會自動淘汰**：快取以連線的伺服器位址分開命名空間（不同伺服器不共用檔案，避免內容雜湊碰撞被拿來換掉別台伺服器的圖），總量上限 128 MB，超過時以最久沒用到的優先、在背景分批淘汰，目前公告用到的圖不會被動到。另有每 30 分鐘 64 MB 的寫入速率上限，撞到只影響當下的時間窗、下一個窗自動恢復
- **圖片同步中斷會續傳**：傳輸卡住時保留已收到的分塊，重試只向伺服器要「第一個缺的分塊」之後的部分，不再整張從頭重來（同一次遊戲程序內有效；離開遊戲後沒下載完的圖仍會重新下載，引擎的檔案輸出沒有 append 能力）。拼回來的內容照樣要通過完整的內容雜湊驗證才會採用。撞到寫入速率上限時，玩家會看到一則「同步已暫停、約 30 分鐘後恢復」的遊戲內提示，一個時間窗最多一則
- **連結**：點擊複製網址到剪貼簿，無效 URL 一律擋下
- **多語系**：伺服器依 `NoticeBoard/<LANG>/` 分語系目錄供稿，找不到時回退到預設語系；玩家可在面板自行選擇公告語系（含「自動（跟隨遊戲語系）」），選擇記在本機 `NoticeBoard/settings.ini`，下次進場沿用
- **更新通知**：以內容 hash 判定未讀，公告改動時跳 Toast、浮窗顯示紅點、對應頁籤標記未讀
- **沙盒選項**：自動彈出模式（一律／僅未讀／不彈出）、輪詢間隔、預設語系、單張圖片大小上限、圖片張數上限、圖片總量上限
- 多人與單機皆支援
