[h1]Minidoracat Notice Board for B42 42.20.2-0.1.0[/h1]
[i]2026-08-25[/i]

[h3]✨ 新增[/h3]
[list]
[*] [b]初始版本[/b]：遊戲內公告欄，直接編輯 Markdown 檔案即可更新（依伺服器輪詢間隔生效，預設 60 秒），多份檔案自動分頁
[*] [b]公告面板[/b]：可拖曳的浮窗按鈕開關面板；視窗大小與位置自動記憶，另有「重設大小」按鈕還原成畫面比例的預設尺寸
[*] [b]Markdown 渲染[/b]：標題、粗體、斜體、行內程式碼、清單、分隔線、連結與圖片（支援的語法與已知差異列在 docs/ADMIN_GUIDE.md）
[*] [b]圖片[/b]：伺服器把 NoticeBoard/images/ 的 PNG 分批同步到玩家本機快取，亦可改用圖包 MOD 的貼圖路徑；支援 ![alt](path =600x200) 尺寸語法（=600x／=x200 依原圖比例補另一邊），指定值會被夾在面板可用寬度內
[*] [b]換圖自動生效[/b]：放進新檔名的圖下一輪輪詢就生效；[b]原地覆蓋同名檔案[/b]也會自動偵測（比對檔案大小），不必再按「重新載入」。新舊檔位元組數剛好相同時仍需手動重新載入
[*] [b]images 資料夾說明檔[/b]：伺服器啟動時在 NoticeBoard/images/ 放一份四語（EN／繁中／简中／日本語）00_README.txt，寫明檔名規則、容量上限、公告內引用寫法與更新機制；已存在則絕不覆蓋
[*] [b]圖片上限可由服主調整[/b]：張數與總量從寫死改為 Sandbox 選項 MaxImageCount（1–200，預設 20）與 MaxImageTotalKB（4096–16384 KB，預設 4096），連同既有的 MaxImageKB（64–4096 KB，預設 512）共三項。[b]預設值即舊行為，什麼都不設完全不變[/b]；超限的 log 會一併寫出當下生效的上限。客戶端保有不受伺服器設定影響的保護上限（接收端張數／單張大小／總量天花板、分塊數必須與宣告位元組數相符，以及快取總量與寫入速率上限）
[*] [b]圖片快取有界且會自動淘汰[/b]：快取以連線的伺服器位址分開命名空間（不同伺服器不共用檔案，避免內容雜湊碰撞被拿來換掉別台伺服器的圖），總量上限 128 MB，超過時以最久沒用到的優先、在背景分批淘汰，目前公告用到的圖不會被動到。另有每 30 分鐘 64 MB 的寫入速率上限，撞到只影響當下的時間窗、下一個窗自動恢復
[*] [b]圖片同步中斷會續傳[/b]：傳輸卡住時保留已收到的分塊，重試只向伺服器要「第一個缺的分塊」之後的部分，不再整張從頭重來（同一次遊戲程序內有效；離開遊戲後沒下載完的圖仍會重新下載，引擎的檔案輸出沒有 append 能力）。拼回來的內容照樣要通過完整的內容雜湊驗證才會採用。撞到寫入速率上限時，玩家會看到一則「同步已暫停、約 30 分鐘後恢復」的遊戲內提示，一個時間窗最多一則
[*] [b]連結[/b]：點擊複製網址到剪貼簿，無效 URL 一律擋下
[*] [b]多語系[/b]：伺服器依 NoticeBoard/<LANG>/ 分語系目錄供稿，找不到時回退到預設語系；玩家可在面板自行選擇公告語系（含「自動（跟隨遊戲語系）」），選擇記在本機 NoticeBoard/settings.ini，下次進場沿用。伺服器掃到的語系目錄清單有變化時會寫一行 log，服主據此就知道該把預設語系設成什麼
[*] [b]語系限定公告[/b]：檔名加上 .only 標記（例如 EN/20_en_notice.only.txt）即代表「只給這個語系目錄的玩家看」，不會被鋪成其他語系的底稿
[*] [b]更新通知[/b]：以內容 hash 判定未讀，公告改動時跳 Toast、浮窗顯示紅點、對應頁籤標記未讀
[*] [b]新公告提示音[/b]：有新公告或公告更新時，除了畫面上的提示訊息再播一聲 MOD 自帶的提示音（音檔可由服主自行替換，見 docs/ADMIN_GUIDE.md「換掉提示音」）。[b]一批只響一次[/b]，不會每則公告都響；換公告語系不響（那不是新內容）
[*] [b]玩家端音效設定[/b]：遊戲的「選項 → MODS」分頁有本 MOD 的提示音開關與音量滑桿（0-100，預設 60）。MOD 自帶音效不受遊戲內建的音效音量影響，這是唯一調得動它的地方；沙盒選項「新公告播放提示音」是服主端總開關，兩者是 AND 關係
[*] [b]沙盒選項[/b]：自動彈出模式（一律／僅未讀／不彈出）、輪詢間隔、預設語系（29 個 PZ 語系代碼下拉）、新公告播放提示音、單張圖片大小上限、圖片張數上限、圖片總量上限
[*] 多人與單機皆支援
[/list]
